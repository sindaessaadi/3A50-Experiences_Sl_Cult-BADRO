{% extends 'base.html.twig' %}

{% block title %}Participation index{% endblock %}

{% block body %}
    <h1>Participation index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id_participant</th>
                <th>Id_event</th>
                <th>Nom_participant</th>
                <th>Date_inscription</th>
                <th>Evenement_nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for participation in participations %}
            <tr>
                <td>{{ participation.id }}</td>
                <td>{{ participation.idEvent }}</td>
                <td>{{ participation.nomParticipant }}</td>
                <td>{{ participation.dateInscription ? participation.dateInscription|date('Y-m-d') : 'N/A' }}</td>
                <td>{{ participation.EvenementNom }}</td>
                <td>
                    <a href="{{ path('participation_show', {'id': participation.id}) }}">View</a>
                    <form method="post" action="{{ path('participation_delete', {'id': participation.id}) }}" onsubmit="return confirm('Are you sure?');">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ participation.id) }}">
                        <button type="submit">Cancel Ticket</button>
                    </form>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">No participations found.</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('evenement_index') }}">Back to Events</a>
{% endblock %}
